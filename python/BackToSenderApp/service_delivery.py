
def service_delivery(user_delivery):

		user_bonus = [160, 200, 250, 500];
		base_pay = 5000;
	
		for count in range(len(user_bonus)):
			if (user_delivery == user_bonus(count)):
				user_commision = user_delivery;
			
		return user_commision;
		
	