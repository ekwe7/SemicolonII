	

for(let count = 1; count <= 2; count++){
    for (let counter = 1; counter <= 9; counter++){
        let output = count * counter;
        console.log(`${count} × ${counter} = ${output}`);
    }console.log();

    
}